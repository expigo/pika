services:
  db:
    image: postgres:16-alpine
    container_name: pika-db
    environment:
      POSTGRES_USER: pika
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pika_secure_change_me}
      POSTGRES_DB: pika_prod
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432" # Allow SSH Tunneling
    restart: unless-stopped
    # 🏥 HEALTHCHECK: Is Postgres ready to accept connections?
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U pika -d pika_prod" ]
      interval: 5s
      timeout: 5s
      retries: 5

  cloud:
    image: oven/bun:latest
    container_name: pika-cloud
    working_dir: /app/packages/cloud
    command: sh -c "cd /app && bun install && cd packages/cloud && bun run start:prod"
    environment:
      - DATABASE_URL=postgres://pika:${POSTGRES_PASSWORD:-pika_secure_change_me}@db:5432/pika_prod
      - NODE_ENV=production
      - PORT=3001
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}
      - VAPID_SUBJECT=${VAPID_SUBJECT}
    volumes:
      - ./:/app
    ports:
      - "127.0.0.1:3001:3001"
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy # 🛑 Wait until DB is green!

  web:
    image: oven/bun:latest
    container_name: pika-web
    working_dir: /app/packages/web
    command: sh -c "cd /app && bun install && cd packages/web && bun run build && bun start"
    environment:
      - NEXT_PUBLIC_CLOUD_WS_URL=wss://api.pika.stream/ws
      - NEXT_PUBLIC_CLOUD_API_URL=https://api.pika.stream
      - NEXT_PUBLIC_VAPID_PUBLIC_KEY=${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./:/app
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    depends_on:
      - cloud

volumes:
  postgres_data:
